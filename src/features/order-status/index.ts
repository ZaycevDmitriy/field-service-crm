// Публичный API фичи order-status (@/features/order-status). Только model (без ui — решение 3 плана):
// беспоковый CTA-бар остаётся версткой экрана pages/order-details.
export { useOrderStatusActions, type IOrderStatusActions } from './model/use-order-status-actions';
